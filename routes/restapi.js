var express = require('express');
var router = express.Router();
var neo_server = require('../lib/neo_server.js');

router.get('/serverinfo/:HospNum', function(req, res){

});

module.exports = router;
